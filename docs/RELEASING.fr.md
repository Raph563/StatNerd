# Release

1. Mettre a jour `addon/VERSION`.
2. Mettre a jour la version runtime dans `addon/dist/custom_js.html`.
3. Commit + tag `vX.Y.Z`.
4. Push du tag.

Asset release:
- `statnerd-addon-vX.Y.Z.zip`
